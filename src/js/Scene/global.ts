export type TSettings = {
  brushRadius: number;
  brushThreshold: number;
  useThreshold: boolean;
  outlineEdge: number;
  sketchIntensity: number;
  sketchNoiseScale: number;
  sketchNoiseIntensity: number;
  brushNoiseScale: number;
  brushNoiseIntensity: number;
  originalRadius: number;
};
